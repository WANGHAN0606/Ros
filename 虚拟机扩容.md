## 在 VMware 中扩展虚拟硬盘
+ 打开 VMware，选中你的 Ubuntu 虚拟机，但不要开机。
+ 点击菜单栏的 “虚拟机” -> “设置”。
+ 在弹出的窗口中，选择 “硬件” 标签页，然后选中你的 “硬盘”。
+ 在右侧找到并点击 “扩展” 按钮。
+ 在“最大磁盘大小”里，输入一个比原来大的新数值（比如，原来是 20GB，现在改成 40GB）。这个值是硬盘扩容后的总大小。
+ 点击 “扩展”，等它跑完进度条就行。

## 虚拟机命令行扩容
Ubuntu 系统里其实自带了一个命令行扩容工具，不需要下载 ISO。但前提是 VMware 第一步扩展完成后，Ubuntu 能正常开机。
打开终端，执行：
```bash
sudo apt install cloud-guest-utils
sudo growpart /dev/sda 2
sudo resize2fs /dev/sda2
```
按顺序执行完这三条命令后：
终端里会显示安装进度，等它跑完。
如果最后出现 Processing triggers for ... 之类的内容，就说明安装成功了。




